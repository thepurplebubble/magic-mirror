// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./data/dev.db"
}

model Message {
  user      String // the slack user id who sent the message
  hcTs      String @id @unique // the ts from the message in the server the message was sent
  hcChannel String // the channel id where the message was sent
  pbTs      String @unique // the ts from the message in the server the message was mirrored
  pbChannel String // the channel id where the message was mirrored
}

// settings for the app
model Settings {
  // the id of the setting
  id String @id @default(uuid())

  // the setting value
  setting String @unique

  // value of the setting
  value String?

  // or a boolean
  boolean Boolean?
}
